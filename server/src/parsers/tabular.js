import XLSX from 'xlsx'
const norm=s=>(s||'').toString().normalize('NFD').replace(/[\u0300-\u036f]/g,'').toUpperCase().replace(/[^A-Z0-9]/g,'')
const aliases={date:['DATA','DATAMOVIMENTO','DTMOV','DATATRANSACAO'],description:['DESCRICAO','HISTORICO','HIST','DESCRICAOMOVIMENTO'],value:['VALOR','VLR','VALORMOVIMENTO'],credit:['CREDITO','ENTRADA','VALORCREDITO'],debit:['DEBITO','SAIDA','VALORDEBITO'],gross:['VALORBRUTO','BRUTO'],fee:['TAXA','TARIFA'],net:['VALORLIQUIDO','LIQUIDO'],status:['STATUS'],type:['TIPOTRANSACAO','TIPO']}
function pick(headers,key){const a=aliases[key]||[];return headers.find(h=>a.includes(norm(h)))}
function n(v){if(typeof v==='number')return v;return Number(String(v||'0').replace(/R\$/g,'').replace(/\./g,'').replace(',','.').replace(/[^0-9.-]/g,''))||0}
export function parseTabular(buffer){const wb=XLSX.read(buffer,{type:'buffer',cellDates:true});let transactions=[];for(const s of wb.SheetNames){const rows=XLSX.utils.sheet_to_json(wb.Sheets[s],{defval:''});if(!rows.length)continue;const hs=Object.keys(rows[0]);const cols=Object.fromEntries(Object.keys(aliases).map(k=>[k,pick(hs,k)]));for(const r of rows){const desc=r[cols.description]||r[cols.type]||'Lançamento importado';let amount=cols.value?n(r[cols.value]):(cols.credit?n(r[cols.credit]):-n(r[cols.debit]));const gross=cols.gross?n(r[cols.gross]):null,fee=cols.fee?n(r[cols.fee]):null,net=cols.net?n(r[cols.net]):null;if(gross!==null&&amount===0)amount=gross;if(!amount&&!gross&&!net)continue;const d=r[cols.date];const occurredAt=d instanceof Date?d:(d?new Date(d):new Date());transactions.push({occurredAt,description:String(desc),amount,direction:amount>=0?'ENTRADA':'SAIDA',grossAmount:gross,feeAmount:fee,netAmount:net,raw:{sheet:s,row:r}})}}return {kind:'TABULAR',transactions,confidence:transactions.length?85:25}}
