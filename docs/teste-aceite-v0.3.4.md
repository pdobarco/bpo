# Teste de aceite — Claria v0.3.4

1. Fazer deploy mantendo o PostgreSQL e a `DATABASE_URL` atuais.
2. Confirmar que o build passa por `claria-server` e `claria-client` sem erro TypeScript.
3. Abrir `/api/health` e confirmar `ok: true`.
4. Abrir Arquivos e testar os botões “Escolher pasta” e “Selecionar arquivos”.
5. Abrir “Todos os lançamentos”, expandir uma linha e testar edição de competência e Plano de Contas.
6. Conferir ordenação pelos cabeçalhos e DRE.
